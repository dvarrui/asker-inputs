%map{ :version => '1', :lang => 'es', :context=>'configuracion,vagrant,ejemplos,vagrantfile' }

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile1

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile2

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile3

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile4

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile5

  %code
    %type vagrantfile
    %path vagrantfile/Vagrantfile6
