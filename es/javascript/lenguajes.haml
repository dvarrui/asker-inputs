%map{ :version => '1', :lang => 'es', :context=>'javascript,lenguaje,programación' }

  %concept
    %names Java
    %tags lenguaje,programacion,java
    %table{ :fields => 'características' }
      %row Compilado e interpretado
      %row Multiplataforma
      %row Orientado a objetos
      %row Imperativo
      %row Tipado estático
    %table{ :fields => 'atributo,valor' }
      %row
        %col Extensión de los archivos
        %col java

  %concept
    %names C
    %tags lenguaje,programacion,c
    %table{ :fields => 'características' }
      %row Compilado
      %row No orientado a objetos
      %row Imperativo
      %row Tipado estático
    %table{ :fields => 'atributo,valor' }
      %row
        %col Extensión de los archivos
        %col c

  %concept
    %names HTML, HyperText Markuo Language
    %tags lenguaje,marcas,hipertexto,elaborar,paginas,web
    %def Lenguaje de marcado para la elaboración de páginas web.
    %table{ :fields => 'atributo,valor' }
      %row
        %col Extensión de los archivos
        %col html
